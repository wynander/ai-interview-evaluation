-- Candidate migrations go here as numbered files, e.g.
--   001_add_orders_index.sql
--   002_add_documents_tsv.sql
--   003_add_tickets_dedup.sql
-- Apply them from your seed/startup path or document them in DESIGN.md.
-- Grading re-runs schema.sql + your migrations before seeding.
SELECT 1;
